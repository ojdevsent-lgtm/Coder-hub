const modal=document.querySelector('#modal');const title=document.querySelector('#modalTitle');const text=document.querySelector('#modalText');const input=document.querySelector('#roomName');const action=document.querySelector('#modalAction');const status=document.querySelector('#status');function open(mode){title.textContent=mode==='create'?'Create a room':'Join a room';text.textContent=mode==='create'?'Choose a room name to get started.':'Enter the room name or invite code.';input.placeholder=mode==='create'?'e.g. frontend-team':'Room name or invite code';action.textContent=mode==='create'?'Create room':'Join room';modal.classList.remove('hidden');input.focus()}document.querySelector('#createRoom').onclick=()=>open('create');document.querySelector('#joinRoom').onclick=()=>open('join');document.querySelector('#signin').onclick=()=>{status.textContent='Authentication coming next';setTimeout(()=>status.textContent='Ready to collaborate',1800)};document.querySelector('#close').onclick=()=>modal.classList.add('hidden');modal.onclick=e=>{if(e.target===modal)modal.classList.add('hidden')};action.onclick=()=>{const room=input.value.trim();if(!room){input.focus();return}status.textContent=`Room: ${room}`;modal.classList.add('hidden');input.value=''};input.addEventListener('keydown',e=>{if(e.key==='Enter')action.click()});
